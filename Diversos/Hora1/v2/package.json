{
  "name": "hora-certa",
  "version": "2.0.0",
  "private": true,
  "description": "Relógio online com design tipográfico minimalista: hora, data, progresso do dia, localização e clima.",
  "type": "module",
  "license": "MIT",
  "scripts": {
    "start": "npx --yes serve .",
    "format": "prettier --write .",
    "format:check": "prettier --check .",
    "lint:js": "eslint assets/js sw.js worker test",
    "lint:css": "stylelint \"assets/**/*.css\"",
    "lint:html": "htmlhint \"*.html\"",
    "lint": "npm run lint:js && npm run lint:css && npm run lint:html",
    "test": "vitest run",
    "build:icons": "sharp -i assets/favicon.svg -o assets/icon-192.png resize 192 && sharp -i assets/favicon.svg -o assets/icon-512.png resize 512 && sharp -i assets/og-image.svg -o assets/og-image.png"
  },
  "devDependencies": {
    "eslint": "^9.13.0",
    "globals": "^15.11.0",
    "htmlhint": "^1.1.4",
    "prettier": "^3.3.3",
    "sharp-cli": "^5.1.0",
    "stylelint": "^16.10.0",
    "stylelint-config-standard": "^36.0.1",
    "vitest": "^2.1.4"
  }
}
